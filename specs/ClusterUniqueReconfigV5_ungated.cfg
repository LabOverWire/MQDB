\* The trap: eager re-replication as a separate action, raw group used for majority immediately on join.
\* Expect NoOversell VIOLATED (AddNode -> Seal before EagerTransfer).
SPECIFICATION Spec
CONSTANTS
    Nodes = {n1, n2, n3, n4, n5}
    InitGroup = {n1, n2, n3}
    Gated = FALSE
INVARIANTS
    TypeOK
    NoOversell
    NoUnrecoverableStrand
CHECK_DEADLOCK FALSE
