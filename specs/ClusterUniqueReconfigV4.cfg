\* Today, with raft liveness enforced. Does the strand/oversell survive when the holder-majority can
\* leave only via the legal grow-then-shed path (grow the cluster, crash holders while a live majority
\* survives, rebalance them out)? Expect: still VIOLATED, but only via that longer legal path.
SPECIFICATION Spec
CONSTANTS
    Nodes = {n1, n2, n3, n4, n5}
    InitGroup = {n1, n2, n3}
    JointConsensus = FALSE
INVARIANTS
    TypeOK
    NoOversell
    NoUnrecoverableStrand
CHECK_DEADLOCK FALSE
