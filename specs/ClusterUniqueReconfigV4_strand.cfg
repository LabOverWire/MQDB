\* Isolate the PERMANENT strand under liveness (grow-then-shed): can an uncommitted reservation lose
\* every in-config holder via raft-legal moves only?
SPECIFICATION Spec
CONSTANTS
    Nodes = {n1, n2, n3, n4, n5}
    InitGroup = {n1, n2, n3}
    JointConsensus = FALSE
INVARIANTS
    TypeOK
    NoUnrecoverableStrand
CHECK_DEADLOCK FALSE
